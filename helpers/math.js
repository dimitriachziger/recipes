/**
 * Multiplizirt zwei Zahlen miteinander
 * @param {*} fst erste Zahl
 * @param {*} snd zweite Zahl
 * @returns Ergebnis der Multiplikation
 */
function mult(fst, snd) {
    return fst * snd;
};
module.exports = {
"mult": mult
}